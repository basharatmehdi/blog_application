import React from "react";

const Advertise = ({ style }) => {
  return (
    <div
      className={`bg-gray-700 md:mt-0 rounded-md overflow-hidden flex items-center justify-center text-gray-300 ${style}`}
    >
      Advertisement
    </div>
  );
};

export default Advertise;
